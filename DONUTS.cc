#include <iostream>
#define ull unsigned long long

int main() {
    int t;
    ull n, m, l;
    std::cin >> t;
    while (t--) {
	std::cin >> n >> m;
	for (int i = 0; i < m; ++i) {
	    std::cin >> l;
	}
    }
    return 0;
}
